set-option -sg escape-time 0

# remap prefix from 'C-b' to 'C-a'
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# vi-like pane navigation
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# vi-like pane resize
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# split panes using \ and -
bind \ split-window -h
bind - split-window -v
unbind '"'
unbind %

# remap copy paste keys
unbind [
unbind p
bind y copy-mode
bind p paste-buffer

set-window-option -g mode-keys vi
bind-key -T copy-mode-vi 'v' send -X begin-selection
bind-key -T copy-mode-vi 'y' send -X copy-selection-and-cancel

# use system clipboard
bind C-c run "tmux save-buffer - | xclip -i -sel clipboard"
bind C-v run "tmux set-buffer \"$(xclip -o -sel clipboard)\"; tmux paste-buffer"


# don't rename windows automatically
set-option -g allow-rename off

# Enable mouse mode
set -g mouse on

set -g default-terminal "screen-256color"


# status bar theme
set -g status-bg 'colour235'
# set -g message-command-fg 'colour222'
# set -g status-justify 'centre'
# set -g status-left-length '100'
# set -g status 'on'
# set -g pane-active-border-fg 'colour154'
# set -g message-bg 'colour238'
# set -g status-right-length '100'
# set -g status-right-attr 'none'
# set -g message-fg 'colour222'
# set -g message-command-bg 'colour238'
# set -g status-attr 'none'
# set -g pane-border-fg 'colour238'
# set -g status-left-attr 'none'
# setw -g window-status-fg 'colour121'
# setw -g window-status-attr 'none'
# setw -g window-status-activity-bg 'colour235'
# setw -g window-status-activity-attr 'none'
# setw -g window-status-activity-fg 'colour154'
# setw -g window-status-separator ''
# setw -g window-status-bg 'colour235'
setw -g clock-mode-style 24

 set -g status-left ''
set -g status-right '#[fg=colour154,bg=colour235] ❊ #(cat $HOME/.kube/config | grep current-context | sed "s/[a-z\-]*: kube-[0-9]*_//" | sed "s/_zalan_do//") #[fg=colour232,bg=colour154] %l:%M '
setw -g window-status-format '#[fg=colour154,bg=colour235] #W '
setw -g window-status-current-format '#[fg=colour238,bg=colour154] #W '
