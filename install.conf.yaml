- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.zshcustom: zshcustom/

- shell:
  - [git submodule update --init --recursive, Installing submodules]


