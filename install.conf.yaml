- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.gitignore_global: gitignore_global
    ~/.gemrc: gemrc
    ~/.pip: pip/
    ~/.config/nvim: nvim
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.zshcustom: zshcustom/

- shell:
  - [git submodule update --init --recursive, Installing submodules]


