- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.pip: pip/
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.zshcustom: zshcustom/

- shell:
  - [git submodule update --init --recursive, Installing submodules]


