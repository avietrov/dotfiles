- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.vifm: vifm/
    ~/.zshcustom: zshcustom/

- shell:
  - [git submodule update --init --recursive, Installing submodules]


