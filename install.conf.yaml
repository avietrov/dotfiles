- clean: ['~']

- link:
    ~/.gitconfig: gitconfig
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
    ~/.vifm: vifm/

- shell:
  - [git submodule update --init --recursive, Installing submodules]


