- clean: ['~']

- create:
  - ~/.config

- link:
    ~/.gitignore_global: gitignore_global
    ~/.config/nvim: nvim
    ~/.config/fish: fish
    ~/.config/custom.fish: custom.fish
    ~/.hushlogin: hushlogin
    ~/.config/Brewfile: Brewfile

- shell:
  - [git submodule update --init --recursive, Installing submodules]


